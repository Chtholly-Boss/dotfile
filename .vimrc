" Global settings
""" set file encoding
set encoding=utf-8
""" set how many lines VIM to remember
set history=500
""" when modified outside of vim, automatically reload
set autoread

""" wrap lines
set wrap

""" set line number as relative
set number
set relativenumber

""" enable syntax highlighting
syntax enable

""" set background color
set background=dark

""" set code folding
set foldmethod=syntax

" Edit Settings
""" allow backspacing over everything
set backspace=indent,eol,start

set tabstop=2
set shiftwidth=4
set expandtab
set smarttab

" Linebreak on 500 characters
set lbr
set tw=500

""" enable mouse
set mouse=a

""" enable clipboard
set clipboard=unnamed

""" enable auto complete
set omnifunc=syntaxcomplete#Complete

set ai "Auto indent
set si "Smart indent
set wrap "Wrap lines

" Functional Settings
""" highlight search result
set hlsearch

""" search case insensitive
set ignorecase
set smartcase

""" dynamic search
set incsearch


